# Git 提交错误排查指南

**时间**: 2026-01-19

## 🔍 常见 Git 提交错误及解决方法

### 1. 推送错误（Push Error）

#### 错误：`Permission denied (publickey)`

**原因**: SSH 密钥未配置或未添加到 GitHub

**解决方法**:
1. 检查 SSH 密钥是否存在：
   ```powershell
   ls C:\Users\Administrator\.ssh\id_ed25519_github_new*
   ```

2. 测试 SSH 连接：
   ```powershell
   ssh -T git@github.com-new
   ```

3. 如果连接失败，检查公钥是否已添加到 GitHub

#### 错误：`remote: Invalid username or password`

**原因**: 使用 HTTPS 方式，但认证失败

**解决方法**:
- 改用 SSH 方式（推荐）
- 或使用 Personal Access Token

#### 错误：`failed to push some refs`

**原因**: 远程仓库有新的提交，本地需要先拉取

**解决方法**:
```powershell
git pull origin main --rebase
git push origin main
```

### 2. 提交错误（Commit Error）

#### 错误：`nothing to commit, working tree clean`

**原因**: 没有需要提交的更改

**解决方法**: 这是正常状态，表示所有更改都已提交

#### 错误：文件被忽略但想提交

**原因**: 文件在 `.gitignore` 中被忽略

**解决方法**:
```powershell
# 强制添加被忽略的文件（谨慎使用）
git add -f 文件名

# 或修改 .gitignore 规则
```

### 3. 认证错误（Authentication Error）

#### 错误：`Authentication failed`

**原因**: Git 凭据过期或错误

**解决方法**:
1. 清除旧的凭据：
   ```powershell
   git credential-manager-core erase
   ```

2. 重新配置用户信息：
   ```powershell
   git config --global user.name "slk1061569042-lab"
   git config --global user.email "slk1061569042@gmail.com"
   ```

### 4. 网络错误（Network Error）

#### 错误：`Could not resolve hostname`

**原因**: 网络连接问题或 DNS 解析失败

**解决方法**:
1. 检查网络连接
2. 检查防火墙设置
3. 尝试使用代理（如果在中国大陆）

## 📋 当前 Git 状态检查

### 检查清单

- [ ] Git 用户配置是否正确
- [ ] 远程仓库配置是否正确
- [ ] SSH 密钥是否配置
- [ ] 是否有未提交的更改
- [ ] 是否有推送冲突

### 快速诊断命令

```powershell
# 检查 Git 状态
git status

# 检查远程仓库
git remote -v

# 检查用户配置
git config --get user.name
git config --get user.email

# 测试 SSH 连接
ssh -T git@github.com-new

# 检查提交历史
git log --oneline -5
```

## 🔧 标准提交流程

### 1. 检查状态
```powershell
git status
```

### 2. 添加文件
```powershell
git add .
# 或指定文件
git add 文件名
```

### 3. 提交更改
```powershell
git commit -m "提交说明"
```

### 4. 推送到远程
```powershell
git push origin main
```

## ⚠️ 如果遇到具体错误

请提供：
1. **完整的错误信息**（复制粘贴）
2. **执行的命令**（你输入了什么）
3. **错误发生的步骤**（是在 commit 还是 push 时）

这样我可以提供更精确的解决方案。

---

**提示**: 如果工作区显示 "nothing to commit, working tree clean"，说明所有更改都已提交，这是正常状态。
