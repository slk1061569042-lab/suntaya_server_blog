# Jenkins Credential 说明

**时间**: 2026-01-19

## 🤔 什么是 Credential？

**Credential（凭据）** 是 Jenkins 用来安全存储和管理敏感信息的机制，比如：
- 密码
- SSH 私钥
- API Token
- 证书文件
- 等等

## 🎯 为什么需要 Credential？

### 1. **安全性**
- 敏感信息会被加密存储
- 不会以明文形式出现在配置文件中
- 只有授权用户才能访问

### 2. **复用性**
- 一次配置，多处使用
- 多个 Job 可以共享同一个 Credential
- 修改时只需更新一处

### 3. **管理方便**
- 集中管理所有凭据
- 可以设置不同的权限范围（Global、Job 级别等）
- 可以随时禁用或删除

## 📝 在你的场景中的作用

### 当前情况

你的 Jenkins Job 需要从 GitHub 拉取代码：
```
Repository URL: git@github.com-new:slk1061569042-lab/suntaya_server_blog.git
```

### 问题

使用 SSH 方式连接 GitHub 时，需要：
1. **SSH 私钥**：用于身份验证
2. **用户名**：`git`（GitHub SSH 协议固定使用 `git` 用户）

### 解决方案

通过 Credential 存储你的 SSH 私钥（`id_ed25519_github_new`），Jenkins 在拉取代码时会：
1. 自动读取 Credential 中的私钥
2. 使用这个私钥与 GitHub 进行 SSH 认证
3. 成功认证后拉取代码

## 🔄 工作流程

```
┌─────────────┐
│  GitHub     │
│  Repository │
└──────┬──────┘
       │
       │ SSH 连接请求
       │
       ▼
┌─────────────────┐
│  Jenkins Job    │
│  (拉取代码)     │
└──────┬──────────┘
       │
       │ 需要身份验证
       │
       ▼
┌─────────────────┐
│  Credential     │
│  (存储私钥)     │
└──────┬──────────┘
       │
       │ 使用私钥认证
       │
       ▼
┌─────────────────┐
│  GitHub 验证    │
│  (匹配公钥)     │
└─────────────────┘
       │
       │ ✅ 认证成功
       │
       ▼
┌─────────────────┐
│  代码拉取成功   │
└─────────────────┘
```

## 📋 配置示例

### 不使用 Credential（不安全 ❌）

```xml
<!-- 直接在配置文件中写死私钥 - 不安全！ -->
<credentialsId>直接写私钥内容</credentialsId>
```

### 使用 Credential（安全 ✅）

```xml
<!-- 只引用 Credential 的 ID -->
<credentialsId>github-ssh-key</credentialsId>
```

实际私钥存储在 Jenkins 的加密凭据库中。

## 🔐 Credential 类型

在你的场景中，我们使用：

**SSH Username with private key**
- **Kind**: SSH Username with private key
- **Username**: `git`（GitHub SSH 固定用户名）
- **Private Key**: 你的 `id_ed25519_github_new` 私钥内容
- **ID**: `github-ssh-key`（在配置中引用这个 ID）

## ✅ 总结

**Credential 的作用**：
1. 🔒 **安全存储** SSH 私钥等敏感信息
2. 🔄 **自动认证** Jenkins 拉取代码时自动使用
3. 📦 **集中管理** 所有凭据统一管理
4. 🔁 **复用共享** 多个 Job 可以共享同一个 Credential

**简单理解**：Credential 就是 Jenkins 的"密码管理器"，帮你安全地存储和使用 GitHub 的 SSH 密钥。

---

**下一步**: 在 Jenkins Web UI 中添加这个 Credential，然后 Job 就能自动从 GitHub 拉取代码了！
