# 🔓 解决 Cursor Server 锁文件问题

## 📊 问题现象

连接时出现以下错误：

```
ln: failed to create hard link '/run/user/0/cursor-remote-lock.6ca0869f6193ac0d40fa6ae74ed01260': File exists
Error: Could not acquire lock after multiple attempts
```

## 🔍 问题分析

**原因**: 服务器上存在旧的 Cursor Server 进程或锁文件，导致新的安装无法获取锁。

**常见情况**:
1. 之前的连接异常中断，锁文件未清理
2. Cursor Server 进程仍在运行
3. 下载进程仍在进行中

## ✅ 解决方案

### 方法 1: 使用清理脚本（推荐）

运行清理脚本：

```powershell
.\scripts\clean_cursor_lock.ps1
```

脚本会：
- ✅ 停止所有 Cursor 相关进程
- ✅ 删除锁文件
- ✅ 清理临时文件

### 方法 2: 手动清理（如果脚本失败）

通过 SSH 连接到服务器并执行：

```bash
# 连接到服务器
ssh root@115.190.54.220

# 停止所有 Cursor 进程
pkill -9 -f cursor-server
pkill -9 -f cursor-remote
pkill -9 -f "wget.*cursor"

# 删除锁文件
rm -f /run/user/0/cursor-remote-lock.*

# 清理临时文件
rm -rf /tmp/cursor-*
rm -rf /run/user/0/cursor-remote-*

# 可选：完全清理 Cursor Server 安装
rm -rf /root/.cursor-server
```

### 方法 3: 一行命令清理

在 PowerShell 中执行：

```powershell
ssh root@115.190.54.220 "pkill -9 -f cursor; rm -f /run/user/0/cursor-remote-lock.*; rm -rf /tmp/cursor-* /run/user/0/cursor-remote-*; echo 'Done'"
```

## 📋 清理后的步骤

1. **等待 5-10 秒**，确保进程完全终止
2. **在 Cursor 中重新连接**：
   - 按 `Ctrl+Shift+P`
   - 选择 `Remote-SSH: Connect to Host...`
   - 输入 `root@115.190.54.220`
3. **等待连接建立**

## 🚨 预防措施

### 如果问题频繁出现

1. **检查服务器资源**：
   ```bash
   # 检查磁盘空间
   df -h
   
   # 检查内存
   free -h
   ```

2. **检查是否有多个 Cursor 连接**：
   ```bash
   ps aux | grep cursor
   ```

3. **定期清理**（可选）：
   创建一个定时任务定期清理旧的锁文件

## 📚 相关文档

- [Cursor连接115.190.54.220指南](./Cursor连接115.190.54.220指南.md)
- [解决Cursor连接卡住问题](./解决Cursor连接卡住问题.md)
- [排查远程连接问题](./排查远程连接问题.md)

---

**最后更新**: 2026-01-19
