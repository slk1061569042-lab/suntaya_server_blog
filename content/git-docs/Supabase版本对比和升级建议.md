# 📊 Supabase 版本对比和升级建议

## 🔍 当前版本 vs 最新版本

| 服务 | 当前版本 | 最新版本 | 状态 | 差距 |
|------|---------|---------|------|------|
| **Storage API** | v1.10.1 | v1.25.7 | ⚠️ 较旧 | 落后 15 个小版本 |
| **Studio** | 20240729 | 2025.11.17 | 🔴 很旧 | 落后约 16 个月 |
| **Realtime** | v2.30.23 | v2.34.47 | ⚠️ 较旧 | 落后 4 个小版本 |
| **Postgres Meta** | v0.83.2 | v0.95.2 | ⚠️ 较旧 | 落后 12 个小版本 |
| **GoTrue (Auth)** | v2.158.1 | v2.177.0 | ⚠️ 较旧 | 落后 19 个小版本 |
| **Postgres** | 15.1.1.78 | 17.6.1.071 | 🔴 大版本落后 | PostgreSQL 15 → 17 |
| **PostgREST** | v12.2.0 | 未知 | ❓ 需确认 | - |
| **Edge Runtime** | v1.56.1 | 未知 | ❓ 需确认 | - |
| **Logflare** | 1.4.0 | 未知 | ❓ 需确认 | - |
| **Kong** | 2.8.1 | 未知 | ❓ 需确认 | - |

## ⚠️ 主要问题

1. **Studio 版本过旧** - 2024年7月的版本，缺少近16个月的功能更新和安全补丁
2. **PostgreSQL 大版本落后** - 从 15 升级到 17 需要谨慎处理
3. **多个服务版本较旧** - 可能缺少重要的 bug 修复和安全更新

## ✅ 升级建议

### 升级策略

#### 方案 1: 渐进式升级（推荐）

**阶段 1: 升级非核心服务**
```bash
# 先升级相对独立的服务
- Storage API: v1.10.1 → v1.25.7
- Realtime: v2.30.23 → v2.34.47
- Postgres Meta: v0.83.2 → v0.95.2
- GoTrue: v2.158.1 → v2.177.0
```

**阶段 2: 升级 Studio**
```bash
# Studio 升级相对安全
- Studio: 20240729 → 2025.11.17
```

**阶段 3: 升级数据库（需谨慎）**
```bash
# PostgreSQL 15 → 17 需要数据迁移
- 先备份数据库
- 测试升级流程
- 执行升级
```

### 方案 2: 使用 Supabase CLI 升级

```bash
# 使用 Supabase CLI 检查版本
supabase status

# 更新 docker-compose.yml
# 从 Supabase 官方仓库获取最新配置
git clone https://github.com/supabase/supabase.git
cd supabase/docker
# 复制最新的 docker-compose.yml 到你的项目

# 重新启动服务
docker-compose pull
docker-compose up -d
```

## 📋 升级前检查清单

- [ ] **备份数据库**
  ```bash
  ssh root@115.190.54.220 "docker exec supabase-db pg_dumpall -U postgres > /backup/supabase_backup_$(date +%Y%m%d).sql"
  ```

- [ ] **检查磁盘空间**
  ```bash
  ssh root@115.190.54.220 "df -h"
  ```

- [ ] **检查当前配置**
  ```bash
  ssh root@115.190.54.220 "docker-compose config"
  ```

- [ ] **查看变更日志**
  - 访问 [Supabase GitHub Releases](https://github.com/supabase/supabase/releases)
  - 查看各服务的变更日志

## 🚨 注意事项

### PostgreSQL 15 → 17 升级

⚠️ **重要**: PostgreSQL 大版本升级需要特殊处理：

1. **数据迁移**: 不能直接升级，需要导出/导入数据
2. **扩展兼容性**: 检查所有 PostgreSQL 扩展是否兼容
3. **测试环境**: 先在测试环境验证升级流程

### 推荐升级顺序

1. ✅ 先升级应用层服务（Storage, Realtime, Auth 等）
2. ✅ 然后升级 Studio
3. ⚠️ 最后升级数据库（需要充分测试）

## 🔧 快速检查命令

```bash
# 查看所有 Supabase 容器版本
ssh root@115.190.54.220 "docker ps --format 'table {{.Names}}\t{{.Image}}' | grep supabase"

# 查看容器健康状态
ssh root@115.190.54.220 "docker ps --format 'table {{.Names}}\t{{.Status}}' | grep supabase"

# 查看 Supabase 配置
ssh root@115.190.54.220 "cat /path/to/your/docker-compose.yml"
```

## 📚 参考资源

- [Supabase Docker 部署文档](https://supabase.com/docs/guides/hosting/docker)
- [Supabase GitHub 仓库](https://github.com/supabase/supabase)
- [PostgreSQL 升级指南](https://www.postgresql.org/docs/current/upgrading.html)

## 💡 建议

**建议尽快升级**，特别是：
1. Studio - 获得最新的管理界面功能
2. Storage API - 获得安全补丁和性能改进
3. Auth (GoTrue) - 获得最新的认证功能和安全修复

**PostgreSQL 升级可以稍后**，因为大版本升级风险较高，需要充分准备。

---

**最后更新**: 2026-01-19
