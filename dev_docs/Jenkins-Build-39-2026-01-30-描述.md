# Jenkins 构建 #39 描述（永久保留）

本文档永久保留本次编译的描述与元信息。

## 构建元信息

| 项 | 值 |
|----|-----|
| **构建编号** | #39 |
| **开始时间** | 2026年1月30日 07:52:50 |
| **触发方式** | GitHub push by slk1061569042-lab |
| **耗时** | 排队 7.8 秒，构建 5 分 21 秒，总计约 5 分 29 秒 |
| **修订版本** | `c0c74e052b84b5c4653d0e67ca0446586cf86404` |
| **远程分支** | `refs/remotes/origin/main` |
| **仓库** | git@github.com:slk1061569042-lab/suntaya_server_blog.git |

## 变更说明

- **主题**：修复 Jenkinsfile 中嵌套部署目录的问题，并更新文档以更清晰地说明远程目录配置。
- **要点**：
  - 解决 Publish Over SSH 将 `remoteDirectory` 拼到默认路径下导致的嵌套目录（如 `www/wwwroot/next.sunyas.com` 出现在部署目录内）。
  - 部署阶段增加检测与展平逻辑，确保应用文件在预期根目录。
  - 文档中明确远程目录配置方式，避免后续误配。

## 保留说明

本次构建在仓库中通过本文档永久保留描述，便于日后追溯与审计。
